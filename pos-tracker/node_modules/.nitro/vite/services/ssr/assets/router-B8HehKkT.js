import { QueryClientProvider, QueryClient } from "@tanstack/react-query";
import { createRootRouteWithContext, useRouter, Link, Outlet, HeadContent, Scripts, createFileRoute, lazyRouteComponent, createRouter } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { useState, useEffect, createContext, useContext } from "react";
const appCss = "/assets/styles-KUgICKrF.css";
function reportLovableError(error, context = {}) {
  if (typeof window === "undefined") return;
  window.__lovableEvents?.captureException?.(
    error,
    {
      source: "react_error_boundary",
      route: window.location.pathname,
      ...context
    },
    {
      mechanism: "react_error_boundary",
      handled: false,
      severity: "error"
    }
  );
}
const TAX_RATE = 0.07;
function calcTotals(items) {
  const subtotal = items.reduce((s, i) => s + i.price * i.quantity, 0);
  const tax = subtotal * TAX_RATE;
  return { subtotal, tax, total: subtotal + tax };
}
const Ctx = createContext(null);
const VALID_PIN = "1234";
const STORAGE = "restaurant_state_v1";
function initialTables() {
  return Array.from({ length: 16 }, (_, i) => ({
    id: i + 1,
    number: i + 1,
    status: "FREE"
  }));
}
function RestaurantProvider({ children }) {
  const [employeeId, setEmployeeId] = useState(null);
  const [tables, setTables] = useState(initialTables);
  const [bills, setBills] = useState([]);
  useEffect(() => {
    try {
      const raw = localStorage.getItem(STORAGE);
      if (raw) {
        const data = JSON.parse(raw);
        if (data.tables) setTables(data.tables);
        if (data.bills)
          setBills(
            data.bills.map((b) => ({
              ...b,
              createdAt: new Date(b.createdAt)
            }))
          );
        if (data.employeeId) setEmployeeId(data.employeeId);
      }
    } catch {
    }
  }, []);
  useEffect(() => {
    localStorage.setItem(
      STORAGE,
      JSON.stringify({ employeeId, tables, bills })
    );
  }, [employeeId, tables, bills]);
  const login = (pin) => {
    if (pin === VALID_PIN) {
      setEmployeeId("emp_001");
      return true;
    }
    return false;
  };
  const logout = () => setEmployeeId(null);
  const placeOrder = (tableId, items) => {
    if (!employeeId || items.length === 0) return;
    const { subtotal, tax, total } = calcTotals(items);
    const bill = {
      id: `bill_${Date.now()}_${Math.random().toString(36).slice(2, 7)}`,
      tableId,
      employeeId,
      items,
      subtotal,
      tax,
      total,
      status: "PENDING",
      createdAt: /* @__PURE__ */ new Date()
    };
    setBills((b) => [...b, bill]);
    setTables(
      (ts) => ts.map(
        (t) => t.id === tableId ? { ...t, status: "OCCUPIED", currentBillId: bill.id } : t
      )
    );
  };
  const addToBill = (billId, newItems) => {
    setBills(
      (bs) => bs.map((b) => {
        if (b.id !== billId) return b;
        const merged = [...b.items];
        for (const it of newItems) {
          const existing = merged.find((m) => m.menuItemId === it.menuItemId);
          if (existing) existing.quantity += it.quantity;
          else merged.push({ ...it });
        }
        const { subtotal, tax, total } = calcTotals(merged);
        return { ...b, items: merged, subtotal, tax, total };
      })
    );
  };
  const payBill = (billId) => {
    let tableId = null;
    setBills(
      (bs) => bs.map((b) => {
        if (b.id === billId) {
          tableId = b.tableId;
          return { ...b, status: "PAID" };
        }
        return b;
      })
    );
    if (tableId !== null) {
      setTables(
        (ts) => ts.map(
          (t) => t.id === tableId ? { ...t, status: "FREE", currentBillId: void 0 } : t
        )
      );
    }
  };
  const getBill = (id) => bills.find((b) => b.id === id);
  return /* @__PURE__ */ jsx(
    Ctx.Provider,
    {
      value: {
        employeeId,
        tables,
        bills,
        login,
        logout,
        placeOrder,
        addToBill,
        payBill,
        getBill
      },
      children
    }
  );
}
function useRestaurant() {
  const ctx = useContext(Ctx);
  if (!ctx)
    throw new Error("useRestaurant must be used inside RestaurantProvider");
  return ctx;
}
function NotFoundComponent() {
  return /* @__PURE__ */ jsx("div", { className: "flex min-h-screen items-center justify-center bg-background px-4", children: /* @__PURE__ */ jsxs("div", { className: "max-w-md text-center", children: [
    /* @__PURE__ */ jsx("h1", { className: "text-7xl font-bold text-foreground", children: "404" }),
    /* @__PURE__ */ jsx("h2", { className: "mt-4 text-xl font-semibold text-foreground", children: "Page not found" }),
    /* @__PURE__ */ jsx("p", { className: "mt-2 text-sm text-muted-foreground", children: "The page you're looking for doesn't exist or has been moved." }),
    /* @__PURE__ */ jsx("div", { className: "mt-6", children: /* @__PURE__ */ jsx(
      Link,
      {
        to: "/",
        className: "inline-flex items-center justify-center rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground transition-colors hover:bg-primary/90",
        children: "Go home"
      }
    ) })
  ] }) });
}
function ErrorComponent({ error, reset }) {
  console.error(error);
  const router2 = useRouter();
  useEffect(() => {
    reportLovableError(error, { boundary: "tanstack_root_error_component" });
  }, [error]);
  return /* @__PURE__ */ jsx("div", { className: "flex min-h-screen items-center justify-center bg-background px-4", children: /* @__PURE__ */ jsxs("div", { className: "max-w-md text-center", children: [
    /* @__PURE__ */ jsx("h1", { className: "text-xl font-semibold tracking-tight text-foreground", children: "This page didn't load" }),
    /* @__PURE__ */ jsx("p", { className: "mt-2 text-sm text-muted-foreground", children: "Something went wrong on our end. You can try refreshing or head back home." }),
    /* @__PURE__ */ jsxs("div", { className: "mt-6 flex flex-wrap justify-center gap-2", children: [
      /* @__PURE__ */ jsx(
        "button",
        {
          onClick: () => {
            router2.invalidate();
            reset();
          },
          className: "inline-flex items-center justify-center rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground transition-colors hover:bg-primary/90",
          children: "Try again"
        }
      ),
      /* @__PURE__ */ jsx(
        "a",
        {
          href: "/",
          className: "inline-flex items-center justify-center rounded-md border border-input bg-background px-4 py-2 text-sm font-medium text-foreground transition-colors hover:bg-accent",
          children: "Go home"
        }
      )
    ] })
  ] }) });
}
const Route$1 = createRootRouteWithContext()(
  {
    head: () => ({
      meta: [
        { charSet: "utf-8" },
        { name: "viewport", content: "width=device-width, initial-scale=1" },
        { title: "Lovable App" },
        { name: "description", content: "Lovable Generated Project" },
        { name: "author", content: "Lovable" },
        { property: "og:title", content: "Lovable App" },
        { property: "og:description", content: "Lovable Generated Project" },
        { property: "og:type", content: "website" },
        { name: "twitter:card", content: "summary" },
        { name: "twitter:site", content: "@Lovable" }
      ],
      links: [
        {
          rel: "stylesheet",
          href: appCss
        }
      ]
    }),
    shellComponent: RootShell,
    component: RootComponent,
    notFoundComponent: NotFoundComponent,
    errorComponent: ErrorComponent
  }
);
function RootShell({ children }) {
  return /* @__PURE__ */ jsxs("html", { lang: "en", children: [
    /* @__PURE__ */ jsx("head", { children: /* @__PURE__ */ jsx(HeadContent, {}) }),
    /* @__PURE__ */ jsxs("body", { children: [
      children,
      /* @__PURE__ */ jsx(Scripts, {})
    ] })
  ] });
}
function RootComponent() {
  const { queryClient } = Route$1.useRouteContext();
  return /* @__PURE__ */ jsx(QueryClientProvider, { client: queryClient, children: /* @__PURE__ */ jsx(RestaurantProvider, { children: /* @__PURE__ */ jsx(Outlet, {}) }) });
}
const $$splitComponentImporter = () => import("./index-Boh8I0U-.js");
const Route = createFileRoute("/")({
  head: () => ({
    meta: [{
      title: "Table Manager — Restaurant POS"
    }, {
      name: "description",
      content: "Mobile-friendly restaurant table and bill management app for servers."
    }, {
      property: "og:title",
      content: "Table Manager — Restaurant POS"
    }, {
      property: "og:description",
      content: "Mobile-friendly restaurant table and bill management app for servers."
    }]
  }),
  component: lazyRouteComponent($$splitComponentImporter, "component")
});
const IndexRoute = Route.update({
  id: "/",
  path: "/",
  getParentRoute: () => Route$1
});
const rootRouteChildren = {
  IndexRoute
};
const routeTree = Route$1._addFileChildren(rootRouteChildren)._addFileTypes();
const getRouter = () => {
  const queryClient = new QueryClient();
  const router2 = createRouter({
    routeTree,
    context: { queryClient },
    scrollRestoration: true,
    defaultPreloadStaleTime: 0
  });
  return router2;
};
const router = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  getRouter
}, Symbol.toStringTag, { value: "Module" }));
export {
  calcTotals as c,
  router as r,
  useRestaurant as u
};
